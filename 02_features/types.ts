// type sample

// type = Date
const today = new Date();
today.getMonth();

// erro on invalid method
// today.NotExistMethod();

// type = { age: 20 }
const person = { age: 20 };

// type = Color
class Color {}
const color = new Color();
